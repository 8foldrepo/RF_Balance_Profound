#Convert .ui file to .py (files made with QT designer to widget library file)
#CAUTION: target .py file will be overwritten

cd widget_library

python -m PyQt5.uic.pyuic -x [FILENAME].ui -o [FILENAME].py

#if image paths don't work, remove the '../' from their path

#Create environment mac with these commands (open terminal with rosetta 2 if needed)

Create new conda environment in pycharm (python 3.10)
pip3 install --upgrade pip
pip3 install PyQt5
pip3 install pyqtgraph
pip3 install pyyaml
pip3 install pyserial
pip3 install -U pyvisa-py
